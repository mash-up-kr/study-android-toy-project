<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.example.toyproject2020mvvm.viewmodel.MainViewModel" />

        <variable
            name="pos"
            type="Integer" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{(v) -> viewModel.seeDetailRepo(v,pos)}"
        tools:context="com.example.toyproject2020mvvm.ui.RepositoryDetailActivity">

        <ImageView
            android:id="@+id/iv_recyclerview_item_GitAvatarImage"
            bind_recycler_pos="@{pos}"
            bind_recycler_viewmodel="@{viewModel}"
            android:layout_width="120dp"
            android:layout_height="120dp"
            tools:layout_editor_absoluteY="25dp"
            tools:srcCompat="@tools:sample/avatars" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="5dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_recyclerview_item_FullName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:text="@{viewModel.repoData[pos].fullName}"
                android:textSize="18sp" />

            <TextView
                android:id="@+id/tv_recyclerview_item_Language"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{viewModel.repoData[pos].language == null ? String.format(@string/no_language) : viewModel.repoData[pos].language}"
                android:textSize="14sp" />
        </LinearLayout>
    </LinearLayout>
</layout>